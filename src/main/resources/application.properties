spring.application.name=code81task

# === Database Configuration ===
spring.datasource.url=jdbc:mysql://localhost:3307/library_management
spring.datasource.username=library_admin
spring.datasource.password=Admin123!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT configuration
jwt.secret=libraryManagementSecretKey
jwt.expiration=86400000

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG

# App server port
server.port=8082

# # Allow circular references (temporarily)
# spring.main.allow-circular-references=true

# # Logging for debugging
# logging.level.code81.code81task=DEBUG
# logging.level.org.springframework.security=DEBUG
# logging.level.org.hibernate.SQL=DEBUG